#!/usr/bin/env bash
# pre-commit hook que actualiza CHANGELOG.md automáticamente con configuración personalizada

if command -v git-changelog >/dev/null 2>&1; then
  echo "🔄 Generando CHANGELOG.md..."
  git-changelog -o CHANGELOG.md --config-file .git-changelog.toml

  if ! git diff --quiet -- CHANGELOG.md; then
    git add CHANGELOG.md
    echo "✅ CHANGELOG.md actualizado y agregado al commit."
  fi
else
  echo "⚠️ git-changelog no está instalado. Ejecuta: pip install git-changelog"
fi
